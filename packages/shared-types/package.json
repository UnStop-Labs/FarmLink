{
  "name": "@farmlink/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for the FarmLink platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
